{
  "name": "sistema-chamada-anjo-guarda",
  "version": "1.0.0",
  "description": "Sistema de Chamada/Frequência - Associação Anjo da Guarda",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\"",
    "install-all": "cd backend && npm install && cd ../frontend && npm install && cd .. && npm install",
    "start-backend": "cd backend && npm start",
    "start-frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "vercel-build": "cd frontend && npm run build"
  },
  "dependencies": {
    "@libsql/client": "^0.6.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "optionalDependencies": {
    "better-sqlite3": "^11.3.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
